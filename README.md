## Configuration
This project requires a private API endpoint and an auth token.
For security reasons, these settings are not included in the repository.

Before running the app, create a file `local.properties` or add your credentials
to your global `gradle.properties`:
